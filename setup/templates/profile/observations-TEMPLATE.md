# Profile — observations

<!-- Behavioral signals OBSERVED during sessions, proposed by the assistant
     and accepted by the user (harvesting step 5). Kept separate from
     declared entries by design: the declared and the observed never mix.
     Recurring observations with accumulating evidence may be proposed for
     promotion to a declared entry at weekly review — the user decides. -->

## <short claim, e.g. "Money decisions tend to get postponed">

- source: observed
- confidence: low         # starts low; evidence raises it
- since: YYYY-MM-DD
- last_challenged:
- evidence:
  - YYYY-MM-DD — <session/area, what was observed>
