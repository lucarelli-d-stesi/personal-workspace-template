# Private instances live here (each is its own git repo) — never tracked
personal/

# Local pointer to the user's instance repo (per machine, per user)
.pos-config

# Local LLM settings
.claude/settings.local.json

# Editor cruft
*.swp
.DS_Store

# Local machine-specific reference links (e.g. to STeSI on dev machine)
reference/
knowledge/stesi-odoo-reference

# Local semantic vector index
.zvec-grep/

# Instance-specific skills linked locally from personal repo
.agents/skills/odoo-*


